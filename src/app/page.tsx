import { TributeStage } from "@/components/TributeStage";

export default function TributePage() {
  return (
    <main className="w-screen h-screen overflow-hidden bg-black">
      <TributeStage />
    </main>
  );
}
